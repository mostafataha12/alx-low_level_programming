#include <stdio.h>
/**
 * main - Print alphabet in lowercase
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;
	
	c = 'a';
	while
		(c <= 'z') {
			if ((c != 'q' && c != 'e') && c <= 'z')
				putcher(c);
			c++;
		}
	putcher('\n');
	return (0);
}
